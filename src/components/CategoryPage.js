import React from 'react';

export const CategoryPage = (props) => (
    <div>
        <h2>Category Type {props.params.categoryID} Events near Toronto, Ontario</h2>
        <div>List of Event Items Component goes here</div>
    </div>
);